# ============================================================
# 학습공동체 3회차 (session3) — 의존성
# ============================================================
# session2 에서 코랩으로 검증된 핀만 가져온다. 3회차에 안 쓰는 것은 뺀다
# (medmnist / transformers / albumentations 제외).
# 학습에 필요한 pandas·pyyaml·seaborn 은 코랩 사전설치 + ultralytics 의존성으로 해결된다.
# 추측해서 추가하지 말 것 — 셀 2가 실제 버전을 출력하므로 확인된 값만 반영한다.

torch==2.11.0
torchvision==0.26.0
ultralytics==8.4.76      # YOLO11 학습 + 추론
numpy==2.0.2
matplotlib==3.10.0
Pillow==11.3.0
