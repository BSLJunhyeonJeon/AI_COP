# ============================================================
# 학습공동체 4회차 (session4) — 의존성
# ============================================================
# session3 에서 코랩으로 검증된 핀을 그대로 가져오고, 학습에 필요한 것만 둔다.
# 추측해서 추가하지 말 것 — 01 노트북 셀 2가 실제 버전을 출력하므로 확인된 값만 반영한다.
#
# mediapipe 는 여기에 넣지 않는다.
#   -> 02_pose_demo.ipynb 셀 2에서만 설치한다. mediapipe 는 numpy/torch 핀을 덮어쓸 위험이
#      있어 학습 노트북(01)의 환경과 분리한다. 코랩 실행으로 안전이 확인되면 그때 핀을 확정한다.
# scikit-learn 도 넣지 않는다.
#   -> 01 셀 8의 precision/recall/F1 은 numpy 로 직접 계산한다(계산식을 보여주는 게 교육적이고,
#      코랩 사전설치에 의존하지 않는다).

torch==2.11.0
torchvision==0.26.0
ultralytics==8.4.76      # 02 의 YOLO11n-pose 용
numpy==2.0.2
matplotlib==3.10.0
Pillow==11.3.0
