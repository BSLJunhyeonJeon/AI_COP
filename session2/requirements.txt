# ============================================================
# 박사후연구원 학습공동체 2회차 — 베이스라인 의존성 (0단계)
# ============================================================
# 규칙(마스터 규칙 3, 0단계 스펙 §5):
#   - 버전은 "코랩에서 실제로 확인된 값"으로 고정한다. 임의로 추측해 적지 않는다.
#   - 0단계 baseline은 사실상 'GPU 확인용 torch' 뿐이다.
#     다운로드 유틸 등 나머지는 필요해지는 1단계부터 여기에 추가한다.
#
# [torch 핀 — 코랩 셀5에서 확인됨]
#   코랩 빌드는 2.11.0+cu128. 크로스플랫폼(로컬/맥/리눅스) 호환을 위해 빌드 메타(+cu128)는
#   빼고 버전만 고정한다. 코랩은 사전설치본을 그대로 쓰므로 재설치가 트리거되지 않는다.
torch==2.11.0

# ============================================================
# 1단계(데이터 준비)에서 추가 — 혈액세포 데이터 + 시각화
# ============================================================
# 버전은 코랩 셀2 출력에서 확인된 값으로 고정(00_/01_ 노트북과 동일 환경 재현).
#   - medmnist: 코랩 미사전설치 → 설치됨. (분류용 BloodMNIST)
#   - numpy/matplotlib/Pillow: 코랩 사전설치본과 동일 버전(있으면 재설치 안 됨). 로컬 conda 환경에도 동일 적용.
medmnist==3.0.2       # 분류용 BloodMNIST
numpy==2.0.2          # 배열/마스크 처리
matplotlib==3.10.0    # 세 형식 비교 시각화
Pillow==11.3.0        # PNG 입출력 (PIL)

# ============================================================
# 2단계(추론 데모)에서 추가 — 사전학습 모델 추론
# ============================================================
# 버전은 02_inference_demo 셀2 출력에서 확인 후 == 로 고정(현재 미고정, 추측 금지).
# torch==2.11.0 과 호환되는 버전을 쓴다. ultralytics 설치가 기존 torch 를 덮어쓰지 않게(이미 만족하면 그대로 둠).
torchvision      # 분류용 ImageNet 모델 + 전처리 (torch 와 짝)
ultralytics      # YOLO11 감지 (COCO 사전학습)
transformers     # SegFormer 분할 (HuggingFace, ADE20K)
