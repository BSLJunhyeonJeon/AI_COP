# ============================================================
# 박사후연구원 학습공동체 2회차 — 베이스라인 의존성 (0단계)
# ============================================================
# 규칙(마스터 규칙 3, 0단계 스펙 §5):
#   - 버전은 "코랩에서 실제로 확인된 값"으로 고정한다. 임의로 추측해 적지 않는다.
#   - 0단계 baseline은 사실상 'GPU 확인용 torch' 뿐이다.
#     다운로드 유틸 등 나머지는 필요해지는 1단계부터 여기에 추가한다.
#
# [torch 핀이 아직 비어 있는 이유]
#   torch는 코랩 사전설치 버전과 "정확히" 일치해야 코랩에서 재설치(대용량/충돌)가
#   일어나지 않는다. 그 정확한 버전은 코랩에서 한 번 확인해야 알 수 있으므로,
#   이 환경에서 추측해 적지 않고 비워 둔다.
#
#   확정 방법(약 1분):
#     1) 상단 "Open in Colab" 배지로 notebooks/00_setup.ipynb 를 코랩에서 연다.
#     2) 셀 1~5 를 위에서부터 ▶ 실행한다.
#     3) 셀 5 출력의 "torch 버전" 값을 그대로 아래 줄에 적고 맨 앞 '#' 을 지운다.
#        예) torch==2.6.0+cu124
#
# torch==<코랩_셀5에서_확인한_정확한_버전>
